{
  "name": "@kajix/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Kajix projects",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "type-check": "tsc --noEmit",
    "verify": "npm run type-check && npm run build"
  },
  "files": [
    "dist"
  ],
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.3"
  }
  
} 